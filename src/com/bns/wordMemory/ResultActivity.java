package com.bns.wordMemory;import java.util.*;import android.app.*;import android.content.*;import android.database.sqlite.*;import android.os.*;import android.view.*;import android.widget.*;import com.bns.Controller.*;import com.bns.Database.*;public class ResultActivity extends Activity {	RACustomAdapter adapter = null;	ListView listView = null;		@Override	public boolean onCreateOptionsMenu(Menu menu) {		// TODO Auto-generated method stub		menu.add(0,0,0,"RETURN MAIN VIEW");		return true;	}	@Override	public boolean onOptionsItemSelected(MenuItem item) {		// TODO Auto-generated method stub		int id = item.getItemId();		switch(id)		{		case 0:			Toast.makeText(this, "Menu Return", Toast.LENGTH_SHORT).show();			return true;		default :			return super.onOptionsItemSelected(item);		}	}	@Override	protected void onCreate(Bundle savedInstanceState) {		// TODO Auto-generated method stub		super.onCreate(savedInstanceState);		this.setContentView(R.layout.result_main);				listView = (ListView)this.findViewById(R.id.listView1);				Intent intent = this.getIntent();		String dbTable = intent.getStringExtra("TableName");				List<String> wordList = new ArrayList<String>();		List<String> matchList = new ArrayList<String>();		DBQueryDate dbqd = new DBQueryDate(dbTable,dbTable+".db",this);		SQLiteDatabase db = dbqd.DBCreate();		wordList = dbqd.getWordList(db);		matchList = dbqd.getMatchList(db);				adapter = new RACustomAdapter(this,0,wordList,matchList);				listView.setAdapter(adapter);	}}