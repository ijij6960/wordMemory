package com.bns.CustomAdapter;import java.util.*;import android.content.*;import android.view.*;import android.widget.*;import com.bns.Eliars.*;public class ResultAdapter extends ArrayAdapter<Boolean> {	private TextView textView = null;	private ImageView imageView = null;	private LayoutInflater _inflater = null;	private List<Boolean> objects = null;	public ResultAdapter(Context context, List<Boolean> objects) {		super(context, 0, objects);		_inflater = (LayoutInflater)context.getSystemService(Context.LAYOUT_INFLATER_SERVICE);		this.objects = objects;	}		@Override	public View getView(int position, View convertView, ViewGroup parent) {		// TODO Auto-generated method stub		View view = null;				if(convertView == null)		{			view = _inflater.inflate(R.layout.result_item_view, null);		}		else		{			view = convertView;		}				textView = (TextView)view.findViewById(R.id.textView1);		imageView = (ImageView)view.findViewById(R.id.imageView1);				boolean b = this.objects.get(position);				if(b)		{			textView.setText("NO. " + (position+1));			imageView.setImageResource(R.drawable.check);		}		else		{			textView.setText("NO. " + (position+1));			imageView.setImageResource(R.drawable.cross_marks);		}				return view;	}}