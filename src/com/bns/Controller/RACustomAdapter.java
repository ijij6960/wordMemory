package com.bns.Controller;import java.util.*;import android.content.*;import android.database.sqlite.*;import android.view.*;import android.widget.*;import com.bns.Database.*;import com.bns.wordMemory.*;public class RACustomAdapter extends ArrayAdapter<String> {	private TextView textView = null;	private ImageView imageView = null;	private LayoutInflater _inflater = null;	private List<String> obj = null;	private List<String> obj1 = null;		public RACustomAdapter(Context context, int resource, List<String> objects, List<String>objects1) {		super(context, resource, objects);		_inflater = (LayoutInflater)context.getSystemService(Context.LAYOUT_INFLATER_SERVICE);		this.obj = objects;		this.obj1 = objects1;		}		@Override	public View getView(int position, View convertView, ViewGroup parent) {		// TODO Auto-generated method stub		View view = null;				if(convertView == null)		{			view = _inflater.inflate(R.layout.result_item_view, null);		}		else		{			view = convertView;		}				textView = (TextView)view.findViewById(R.id.textView1);		imageView = (ImageView)view.findViewById(R.id.imageView1);				textView.setText(obj.get(position));		if(obj1.get(position).equals("T"))		{			imageView.setImageResource(R.drawable.check);		}				return view;	}}