package com.bns.Controller;import java.util.*;import android.*;import android.content.*;import android.graphics.*;import android.util.*;import android.view.*;import android.widget.*;import com.bns.wordMemory.R;public class MACustomAdapter extends ArrayAdapter<String> {	private LayoutInflater _inflater;	private ImageButton imageButton;	private TextView textview;	private List<ImageButton> imageButtonList;	private List<String> obj;	public static Map<Integer,String> check = new HashMap<Integer,String>();	public static Map<Integer,String> checkString = new HashMap<Integer,String>();		public MACustomAdapter(Context context, int resource, List<String> objects) {		super(context, resource, objects);		// TODO Auto-generated constructor stub		this._inflater = (LayoutInflater)context.getSystemService(Context.LAYOUT_INFLATER_SERVICE);		this.imageButtonList = new ArrayList<ImageButton>();		this.obj = objects;	}	@Override	public View getView(int position, View convertView, ViewGroup parent) {		// TODO Auto-generated method stub		View view = null;		final int _position = position;		if(convertView == null)		{			view = _inflater.inflate(R.layout.item_view, null);		}		else		{			view = convertView;		}				imageButton = (ImageButton)view.findViewById(R.id.imageButton1);		textview = (TextView)view.findViewById(R.id.textView1);				imageButton.setImageResource(R.drawable.cross_marks);		imageButtonList.add(_position,imageButton);		check.put(_position, "OFF");		checkString.put(_position, obj.get(_position));		imageButton.setOnClickListener(new View.OnClickListener() {						@Override			public void onClick(View v) {				// TODO Auto-generated method stub				for(int index = 0; index < imageButtonList.size();index++)				{					if(index == _position)					{						imageButtonList.get(_position).setImageResource(R.drawable.check);						check.put(_position, "ON");					}					else					{						imageButtonList.get(index).setImageResource(R.drawable.cross_marks);						check.put(index, "OFF");					}				}			}		});				textview.setText(obj.get(_position));				return view;	}}